<div class="w-1/2 m-auto">
  <%= simple_form_for :farm do |f| %>
    <%# <%= f.input :farm, collection: @farms.map { |fa| [fa.code, fa.id, {name: fa.code} ] } %>
    <%= f.input :file, as: :file, label: "CSV" %>
    <%= f.button :submit, 'Upload File' %>
  <% end %>
</div>