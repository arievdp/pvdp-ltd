<tr>
  <th class="mb-4 text-xs font-extrabold tracking-wider flex flex-row items-center"><%= c.birth_id %></th>
  <th class="mb-4 text-xs font-extrabold tracking-wider text-right"><%= c.birth_date.to_date %></th>
  <th class="mb-4 text-xs font-extrabold tracking-wider text-right"><%= c.bw_value %></th>
  <th class="mb-4 text-xs font-extrabold tracking-wider text-right"><%= c.fate %></th>
</tr>
